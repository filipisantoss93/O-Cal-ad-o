"use client";

export function OptimizedImageNote() {
  return (
    <span className="mt-1.5 block text-xs font-semibold text-muted">
      A foto é redimensionada e convertida para WebP no seu aparelho antes do envio, reduzindo o uso de armazenamento e dados.
    </span>
  );
}
